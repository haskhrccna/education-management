node_modules/
.env
.env.local
*.log
dist/
build/
expo/
.expo/
ios/Pods/
android/app/build/
.claude/
.DS_Store

# Server runtime output (user uploads + generated PDFs — never source)
packages/server/uploads/
packages/server/reports/
packages/server/certificates/

# Mushaf page images (~51MB) — generated from a source PDF by
# scripts/extract_mushaf_pages.py, served statically. Regenerate, don't commit.
packages/server/mushaf-pages/

# Tooling scratch output
graphify-out/

# Ignore Flutter/Dart build and tool files
.dart_tool/
.flutter-plugins-dependencies
build/
*.log

# Ignore the specific directories you mentioned
flappy-bird/
lovedheart/
mlx-community/

# Ignore generated localization files (usually best to ignore these)
lib/l10n/app_localizations*.dart

# Usually good to ignore the lock file if this is a library, 
# but keep it if it's an app. If you want it gone:
# pubspec.lock
